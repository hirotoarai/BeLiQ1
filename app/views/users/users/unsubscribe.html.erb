<body>
  <div class="container">
    <div class="saito">
      <div class="row">
        <div class="text-center col-xs-12">
          <h4>退会すると投稿ができなくなります。</h4>
          <h4>本当に退会しますか？</h4>
          <!--ユーザーがカレントユーザーである場合-->
          <% if @user == current_user %>
            <div class="img_wrap">
    		      <h4><%= link_to "退会する", withdraw_user_path(@user), method: :patch %></h4></br>
    		    </div>
    		    <div class="img_wrap">
    		      <h4><%= link_to "マイページに戻る", mypage_path(@user) %></h4>
    		    </div>
    		  <% end %>
    		  <!--アドミンの場合-->
    		  <% if admin_signed_in? %>
    		    <div class="img_wrap">
    		      <h4><%= link_to "退会させる", withdraw_user_path(@user), method: :patch %></h4></br>
    		    </div>
    		    <div class="img_wrap">
    		      <h4><%= link_to "マイページに戻る", user_path(@user) %></h4>
    		    </div>
    		  <% end %>
        </div>
      </div>
    </div>
  </div>
</body>